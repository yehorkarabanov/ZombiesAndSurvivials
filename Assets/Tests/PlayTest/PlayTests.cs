using System.Collections;
using System.Collections.Generic;
using _Scripts.Managers;
using _Scripts.Tile;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class PlayTests {
    [Test]
    public void PlayTestsSimplePasses() {
        // var pathFinder = new PathFinder();
        // var game = new GameObject();
        // game.gameObject.AddComponent<GridManager>();
        // game.gameObject.AddComponent<ItemManager>();
        // game.gameObject.AddComponent<GameManager>();
        // game.gameObject.AddComponent<PostGameManager>();
        // game.gameObject.AddComponent<MoveManager>();
        //
        // var g = game.gameObject.AddComponent<GrassTile>();
        // g._baseColor = Color.green;
        // var w = game.gameObject.AddComponent<WaterTile>();
        // w._baseColor = Color.green;
        // var m = game.gameObject.AddComponent<MountainTile>();
        // m._baseColor = Color.green;
        //
        // GridManager.Instance._height = 20;
        // GridManager.Instance._width = 20;
        // GridManager.Instance._grassTile = g;
        // GridManager.Instance._mountainTile = m;
        // GridManager.Instance._waterTile = w;
        // GridManager.Instance.GenerateGrid();
        // Assert.True(true);
    }
}